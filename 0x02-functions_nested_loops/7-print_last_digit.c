#include "main.h"
/**
  *print_last_digit - print last digit of a number
  *@n: value of last digit
 *Return: n
 */
int print_last_digit(int n)
{
	int a;

	a = n % 10;
	while (n > 0)
	{
		return (a);
	}
	return (0);
}
